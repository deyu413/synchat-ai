{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",  // Cambiado para capturar TODAS las rutas
      "dest": "server.js"
    }
  ]
}
